# Terraform state — local, machine-specific, never commit
terraform.tfstate
terraform.tfstate.backup
.terraform.tfstate.lock.info

# Terraform working directory — regenerated by `terraform init`
.terraform/

# Build artifacts — regenerated by `terraform apply`
.build/

# Sensitive/environment-specific values, if introduced later
*.tfvars

# macOS
.DS_Store